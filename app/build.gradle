apply plugin: 'com.android.application'
apply plugin: 'org.greenrobot.greendao'

android {
    compileSdkVersion 25
    buildToolsVersion "28.0.3"
    aaptOptions.cruncherEnabled = false
    aaptOptions.useNewCruncher = false

    defaultConfig {
        applicationId "com.example.dxnima.zhidao"
        minSdkVersion 16
        targetSdkVersion 25
        versionCode 1
        versionName "1.0"
        //添加如下配置就OK了
        javaCompileOptions { annotationProcessorOptions { includeCompileClasspath = true } }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
       }

    packagingOptions {
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/javamail.providers'
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    testImplementation 'junit:junit:4.12'
    implementation 'com.android.support:appcompat-v7:25.4.0'
    implementation 'com.jakewharton:butterknife:7.0.1'
    implementation 'com.android.support:design:25.4.0'
    implementation 'com.android.support.constraint:constraint-layout:1.0.2'
    implementation 'org.greenrobot:greendao:3.0.1'
    implementation 'org.greenrobot:greendao-generator:3.0.0' //greendao数据库
    implementation 'pl.droidsonroids.gif:android-gif-drawable:1.2.10' //gif
    implementation 'org.greenrobot:eventbus:3.0.0'


    implementation files('libs/eventbus.jar')
    implementation files('libs/gson-2.2.4.jar')
    implementation files('libs/okhttp-2.7.0.jar')
    implementation files('libs/okio-1.6.0.jar')
    implementation files('libs/picasso-2.5.2.jar')
}

greendao {
    schemaVersion 1
    daoPackage 'com.example.dxnima.zhidao.dao'
    targetGenDir 'src/main/java'
}